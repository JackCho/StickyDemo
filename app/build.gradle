buildscript {
    repositories {
        mavenCentral()
        maven { url "https://github.com/ricechen/Eradle/raw/maven/releases" }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.0.0'
        classpath 'me.ele:eradle:1.2'
    }
}

apply plugin: 'com.android.application'
apply plugin: 'maven'
apply plugin: 'eradle'

repositories {
    github("eleme", "EMAndroidMavenCentral", "me.ele_recyclerview_0.3.4", "releases")
}

android {
    compileSdkVersion 21
    buildToolsVersion "21.1.2"

    defaultConfig {
        applicationId "me.ele.homedemo"
        minSdkVersion 14
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'me.ele:recyclerview:0.3.4'
    compile 'me.ele:omniknight:1.3.4'
}
